<!DOCTYPE html>
<html lang="en">    

<head>
    <%- include('../layout/head') %>
  </head>
  
  <body>
    <header>
      <%- include('../layout/header') %>
    </header>
  
        <main>
            <h2>There're  <%= books.length %> Saved Books in the Collection</h2>
            <% books.forEach(book => { %>  
              <div data-book-id="<%= book.id %>">
                  <%- include('books/detail_short', {book: book}) %>
              </div>
              <% }) %>
        </main>
      </body>
</html>