<!DOCTYPE html>
<html lang="en">

    <head>
        <%- include('../../layout/head') %>
      </head>
      
      <body>
        <header>
          <%- include('../../layout/header') %>
        </header>
<main>
  <% searchResults.forEach(item => { %>
  <img src="<%= item.image %>"/>

  <p><%= item.title %></p>

  <p><%= item.author %></p>
 
  <p><%= item.description %></p>
  <% }) %>
</main>
</body>

</html>